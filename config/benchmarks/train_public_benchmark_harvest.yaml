extends: ../default.yaml

runtime:
  safe_mode: true

benchmark:
  assistance_mode: unassisted
  oracle_hints_enabled: false
  fail_on_integrity_violation: true

training:
  benchmark_harvest_cases: 4
  benchmark_recovery_examples_per_step: 2
  benchmark_harvest_suites:
    - train_cases
